import { RendererDirectiveDirective } from './renderer-directive.directive';

describe('RendererDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RendererDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
